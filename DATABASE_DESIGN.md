# تصميم قاعدة البيانات - Multi-Platform Games

## 📊 التصميم المحسّن

### البنية الجديدة

```
platform_games (جدول الألعاب المشتركة)
├── id
├── platform (playstation, xbox, steam, epic)
├── platform_game_id (NPWR09412_00) - UNIQUE per platform
├── name (Red Dead Redemption 2)
├── icon_url
└── metadata (JSON)

user_games (بيانات المستخدم فقط)
├── id
├── user_id
├── platform_account_id
├── platform_game_id (FK → platform_games.id)
├── total_playtime_minutes
├── trophies_* (PlayStation specific)
├── platform_specific_data (JSON - مرن لكل منصة)
└── last_played_at
```

## ✅ الفوائد

1. **تقليل التكرار**: كل لعبة تُخزن مرة واحدة فقط لكل منصة
2. **توفير المساحة**: من 150,000 سجل مكرر إلى ~15,000 لعبة + 150,000 سجل
3. **سهولة التحديث**: تحديث بيانات اللعبة في مكان واحد
4. **دعم متعدد المنصات**: نفس التصميم لجميع المنصات
5. **مرونة**: بيانات خاصة بكل منصة في JSON

## 🚀 كيفية التطبيق

### 1. تشغيل Migrations

```bash
php artisan migrate
```

هذا سيقوم بـ:
- إنشاء جدول `platform_games`
- نقل البيانات الموجودة من `user_games` إلى `platform_games`
- تعديل `user_games` لاستخدام foreign key

### 2. التحقق من البيانات

```bash
# عدد الألعاب في platform_games
php artisan tinker
>>> App\Models\PlatformGame::count()

# عدد سجلات user_games
>>> App\Models\UserGame::count()
```

## 📝 ملاحظات

- البيانات الموجودة سيتم نقلها تلقائياً
- لا حاجة لحذف البيانات الحالية
- Migration آمن ويمكن الرجوع عنه
